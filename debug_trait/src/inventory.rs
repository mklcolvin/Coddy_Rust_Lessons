// TODO: Add the #[derive(Debug)] attribute to make the struct printable with {:?}

// TODO: Define a public Item struct with:
// - name: String (public)
// - quantity: u32 (public)
#[derive(Debug)]

pub struct Item {
    pub name: String,
    pub quantity: u32,
}